/**
 * A function that does foo
 * @param  {Number} baz Lots of baz.
 * @param  {Number} [bah] Some more bah.
 * @example <caption>The description of the example</caption>
 * var res = foo(1,2);
 * console.log(res);
 * @example <caption> The description of the second example.</caption>
 * var res = foo(1);
 * console.log(res);
 * @example
 * console.log(foo(2));
 * @todo Needs more foobah.
 * @todo
 * @return {Boolean} The thing that comes ot of it.
 */
function foo(baz, bah) {
  return true;
}

/**
 * @description The description of the function.
 * This should be multiline! With <b>HTML</b>
 *
 * @param  {String} baz  Makes the beat go baz.
 * @param  {String} [boom] Say boom and bang. Can this also be <b>mulitline?</b>.
 * I hope so.
 * @param  {String} [bang] These aren't the droids you are looking for.
 * @return {String} Nothing to see here move along.
 */
function bahbaz(baz, boom, bang) {
  return 'FOO';
}
